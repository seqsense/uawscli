# µawscli - collection of tiny AWS console client utilities

```shell
# Install latest version of uaws-ecr-get-login-password under ~/.local/bin
$ curl -sL https://raw.githubusercontent.com/seqsense/uawscli/master/install.sh \
  | sh -s ecr-get-login-password

# Install latest version of uaws-ecr-get-login-password under /path/to/bin
$ curl -sL https://raw.githubusercontent.com/at-wat/gh-pr-comment/master/install.sh \
  | sh -s -i /path/to/bin ecr-get-login-password

# Install specific version of uaws-ecr-get-login-password under /path/to/bin
$ curl -sL https://raw.githubusercontent.com/at-wat/gh-pr-comment/master/install.sh \
  | sh -s -v v0.0.0 -i /path/to/bin ecr-get-login-password
```
